import React from 'react';
import "./influencerButton.css";

const InfluncerButton = ({text}) => {
  return (
    <button type='button' className='button'>{text}</button>
  )
}

export default InfluncerButton